[package]
name = "cat"
version = "0.1.0-alpha.1"
edition = "2021"

[dependencies]
moyuan-libc = { path = "../../libs/libc" }

[[bin]]
name = "cat"
path = "src/main.rs"

[target.'cfg(target_os = "none")']
panic = "abort"
