[package]
name = "mkdir"
version = "0.1.0-alpha.1"
edition = "2021"
autotests = false
autobenches = false

[dependencies]
moyuan-libc = { path = "../../libs/libc" }

[[bin]]
name = "mkdir"
path = "src/main.rs"

[target.'cfg(target_os = "none")']
panic = "abort"
