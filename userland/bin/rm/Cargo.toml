[package]
name = "rm"
version = "0.1.0-alpha.1"
edition = "2021"

[dependencies]
moyuan_libc = { path = "../../libs/libc" }

[[bin]]
name = "rm"
path = "src/main.rs"

[target.'cfg(target_os = "none")']
panic = "abort"
